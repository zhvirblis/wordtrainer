import { AnyAction } from "@reduxjs/toolkit";
import { Reducer } from "redux";

const initial = {
}

const reducer: Reducer = function(state = initial, action: AnyAction) {
    return state;
}

export default reducer;